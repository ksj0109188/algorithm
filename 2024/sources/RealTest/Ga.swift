//
//  Ga.swift
//  BakJoon
//
//  Created by 김성준 on 5/11/25.
//

import Foundation

//exapmle n:2, k:10, s[1], e[2], d[15] -> 15출력
//k초과 가중치를 지나면 그 다음에 k이하의 가중치만 갈 수 있다.
// n은 노드의 갯수, s[i], e[i] -> s노드, e노드로 가는 간선이 d[i]만큼 가중치가 있는 간선
// 1 -> n노드로 가는 최소한의 비용 출력, 못가면 -1
// 노드 수는 백만 단위로 -> O(V+E)알고리즘 필요

final class Ga_Q1 {
    func solution(_ n: Int, _ k: Int, _ s: [Int], _ e: [Int] ) {
        
    }
}
